{
  "name": "calendario-app",
  "private": true,
  "version": "0.1.0-beta.1",
  "type": "module",
  "description": "Calendário desktop Windows — widget + app (Tauri + Google Calendar)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
